# === 原版遊戲檔（請玩家自備 legit copy，不散布）===
# DOS 原版資料 / 執行檔
dos-original/
ultima2/
*.exe
ultimaii.pif
mapx*
monx*
tlkx*
mapg*
mong*
tlkg*
MAPG*
MONG*
TLKG*
/monsters
dos-original/**/monsters
# 注:docs/monsters/ 為怪物圖鑑 sprite,需保留(下方放行)
!docs/monsters/
!docs/monsters/**
player
pic*
Font.txt
run.bat

# === Alderson Win 移植版 binary（版權物，不散布）===
Ultima2.exe
WinUltima2.zip
*Readme.txt

# === U2 Upgrade (mcmagi) — 研究用途已獲授權提交 tileset/ 下的 tileset ===
# 來源:https://github.com/mcmagi/ultima-exodus/releases  →  u2upgrade-2.1.zip
# tileset/CGATILES, tileset/EGATILES 已提交(研究/保存用途);其餘解壓工作檔忽略:
u2upgrade*/
build/u2up/
*.zip
# 不提交大型 .EGA 圖檔(非引擎 tileset 必需)
PIC*.EGA
# 注:tileset/EGATHEME.{ALT,C64}/EGATILES 已收錄供 tileset 切換;
# build/u2up 下的解壓暫存已被 build/ 規則忽略。

# === Ghidra 反編 oracle C ===
# 專案裁示:Ultima II 已過版權、公開逆向資料多 → oracle/ 收錄供 CRPG 史保存。
# 僅忽略本機工作目錄;repo 內 oracle/ 已提交。
decompile/
build/u2up/

# === 原始壓縮檔 ===
*.zip

# === 建置產物 ===
build/
dist/
*.o
*.out
*_out.png
compile_commands.json

# === Python ===
__pycache__/
*.pyc

# === 存檔 / 使用者設定（執行期產生）===
*.sav
u2save.dat
u2prefs.txt

# === IDE / OS / agent session ===
.claude/
.vscode/
.idea/
*.swp
.DS_Store
